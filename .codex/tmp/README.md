# Codex 临时产物

这里是 Codex 会话的临时工作区，用于保存本次任务中生成的 `.cs`、`.md`、`.json`、测试脚本和其他中间文件。

## 约定

- 新生成的说明文档、脚本和测试辅助文件先放在 `.codex/tmp/` 或其任务子目录。
- 不要把未经确认的新内容直接写入 `.claude/`；`.claude/` 是 Claude 侧的权威知识体系，由 Claude 决定归档位置并负责去重、同步。
- 写入 `Assets/` 的功能资产按用户任务范围和项目门禁执行。
- 本目录内容不是权威项目知识；任务结束后可由调用方审查、转移或清理。
