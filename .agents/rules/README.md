# 共享规则索引

规则与客户端无关，并按路径或任务类型自动选择。正文已从 `.claude/rules/` 迁入；旧 Claude 规则路径由相对软链兼容。

| 规则 | 适用范围 |
|---|---|
| `shader-development.md` | Shader / HLSL 开发 |
| `csharp-renderpass.md` | C# RenderPass / RendererFeature |
| `compute-shader.md` | Compute Shader |
| `agent-architecture.md` | agent 体系、归属、迁移与去重 |

本阶段仅建立索引骨架；不要在平台目录新增共享规则副本。
