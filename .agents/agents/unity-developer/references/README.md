# Unity references 索引

本目录是 `unity-developer` 的角色归属层。Phase 3 将按迁移清单逐项迁入 references；本阶段不复制 `.claude` 正文。

| 分类 | 用途 |
|---|---|
| `standard/` | Script、Shader、Compute、Rendering 基础规范 |
| `shader/` | 后处理与渲染功能专项知识 |
| `platform/` | Metal 和平台差异 |

读取策略：先读与任务类型匹配的索引，再读取具体 reference；不要按目录全量加载。
