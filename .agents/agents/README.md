# 共享角色索引

角色定义是跨平台 SSOT。平台目录中的 agent manifest 只能映射到这里，不得复制角色正文。

| 角色 | 入口 | 职责 |
|---|---|---|
| `unity-developer` | `unity-developer/AGENT.md` | Unity 6 / URP 17+ 开发、验证与项目规则 |
| `meta-developer` | `meta-developer/AGENT.md` | agent 体系、索引、适配层与引用完整性 |

首轮迁移保留现有角色名。工作模式（Production、Research、Experiment）属于流程，不创建并列角色。
